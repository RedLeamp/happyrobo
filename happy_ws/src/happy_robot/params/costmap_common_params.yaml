map_type: costmap
origin_z: 0.0
z_resolution: 1
z_voxels: 2

# obstacle_range: 2.5
obstacle_range: 100.0
raytrace_range: 100.0
# raytrace_range: 3.0

publish_voxel_map: true
transform_tolerance: 0.5
meter_scoring: true

footprint: [[-0.21, -0.165], [-0.21, 0.165], [0.21, 0.165], [0.21, -0.165]] # small
# footprint: [[-2.85, -1.25], [-2.85, 1.25], [2.85, 1.25], [2.85, -1.25]] # long
# footprint: [[-1.25, -2.85], [1.25, -2.85], [1.25, 2.85], [-1.25, 2.85]] # fat
footprint_padding: 0.1

plugins:
# - {name: static_layer, type: "costmap_2d::StaticLayer"}
- {name: obstacles_layer, type: "costmap_2d::ObstacleLayer"}
- {name: inflater_layer, type: "costmap_2d::InflationLayer"}
# - {name: obstacle, type: "costmap_2d::ObstacleLayer"}
# - {name: inflation, type: "costmap_2d::InflationLayer"}

# obstacle:
#   observation_sources: point_cloud_sensor
#   point_cloud_sensor: {sensor_frame: sigbot_base_01, data_type: PointCloud2, topic: /merged/pointClouds , marking: true, clearing: true, min_obstacle_height: -2.0, max_obstacle_height: 2.0, obstacle_range: 25.0, raytrace_range: 25.0}
#   track_unknown_space: true

# obstacles_layer:
#   observation_sources: point_cloud_sensor
#   point_cloud_sensor: {sensor_frame: sigbot_base_01, data_type: PointCloud2, topic: /merged/pointClouds , marking: true, clearing: true, min_obstacle_height: -2.0, max_obstacle_height: 2.0, obstacle_range: 100.0, raytrace_range: 100.0}
  # point_cloud_sensor:
  #   sensor_frame: sigbot_base_01  # 센서 프레임 (필요에 따라 수정)
  #   data_type: PointCloud2  # LaserScan이 아니라 PointCloud2 사용
  #   topic: /merged/pointClouds  # 사용할 PointCloud2 토픽
  #   marking: false
  #   clearing: true
  #   min_obstacle_height: -12.0
  #   max_obstacle_height: 12.0
obstacles_layer:
  observation_sources: scan
  # scan: {sensor_frame: front_laser, data_type: LaserScan, topic: /merged/scan, marking: false, clearing: true, min_obstacle_height: -2.0, max_obstacle_height: 2.0, obstacle_range: 10.0, raytrace_range: 11.0}
  scan: {sensor_frame: front_laser, data_type: LaserScan, topic: front/scan, marking: false, clearing: true, min_obstacle_height: -2.0, max_obstacle_height: 2.0, obstacle_range: 11.0, raytrace_range: 11.0}
inflater_layer:
 inflation_radius: 0.15
